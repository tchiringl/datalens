# =============================================================================
# Trino Node Properties
# =============================================================================
# node.id must be unique across all nodes in the cluster. For a multi-node
# setup, assign a different UUID to each worker (e.g. use `uuidgen`).
# =============================================================================

node.environment=production
node.id=datahub-trino-node-1
node.data-dir=/data/trino
